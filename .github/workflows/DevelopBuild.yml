name: DevelopBuild

on:
  push:
    branches:
      - develop  # 開発用ブランチを指定
      - Game_master
      - 10Days_master
  workflow_dispatch:

env:
  # 正解のパスを指定
  SOLUTION_FILE_PATH: MyDreamGame/project/MyDreamGame.sln
  CONFIGURATION: Debug  # 開発中なのでDebugビルド

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Add MSBuild to PATH
        uses: microsoft/setup-msbuild@v2
        with:
          msbuild-architecture: x64
      - name: Build
        run: msbuild ${{env.SOLUTION_FILE_PATH}} /p:Platform=x64,Configuration=${{env.CONFIGURATION}}
